#include <png++/png.hpp>
#include <GLFW/glfw3.h>
#include <fmt/format.h>

#include "icon.h"

void setWindowIcon(const std::string& pngIconPath)
{
//  png::image<png::rgb_pixel> image(pngIconPath);
//  fmt::print("h={}\n", image.get_height());
//  glfwSetWindowIcon(); // Available in GLFW 3.2. Latest packaged version is 3.1, so will just wait.
}
